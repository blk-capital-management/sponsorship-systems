# BLK Sponsor Outreach — Python dependencies
# Install with: pip install -r requirements.txt

# Research crawl
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0                # faster, more forgiving parser for BeautifulSoup
jsonschema>=4.20         # enforces research/schema.json

# Data
pandas>=2.0              # inspection
openpyxl>=3.1            # CRM writeback (xlsx backend)

# Drafting
anthropic>=0.40          # firm paragraph generation, hooks-only input

# Google APIs (Gmail drafts, Sheets CRM backend)
google-api-python-client>=2.100
google-auth>=2.22
google-auth-oauthlib>=1.1
google-auth-httplib2>=0.1

# Config / env
pyyaml>=6.0
python-dotenv>=1.0

# Phase G dashboard (Vercel Python Functions)
fastapi>=0.117.1,<1
uvicorn>=0.30,<1

# Testing
pytest>=8.0
httpx>=0.27

# Optional: JS-rendered sites only. Enable research.playwright_fallback in
# settings.yaml, then run `playwright install chromium`.
# playwright>=1.40
